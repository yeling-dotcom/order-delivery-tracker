import {getCustomers}from "@/lib/data/customers";import {formatDate}from "@/lib/utils";
export const dynamic="force-dynamic";
export default async function CustomersPage(){const customers=await getCustomers();return <div className="page-wrap"><header className="page-header split"><div><p className="eyebrow">Directory</p><h1>Customers</h1><p className="subtitle">Contact and delivery details captured from orders.</p></div><a className="button primary" href="/orders/new">+ Add through order</a></header><div className="section-block table-wrap">{customers.length===0?<div className="empty-state"><span>◎</span><h3>No customers yet</h3><p>Customers appear here after their first order.</p></div>:<table className="data-table"><thead><tr><th>Name</th><th>Phone</th><th>Email</th><th>Address</th><th>Added</th></tr></thead><tbody>{customers.map(c=><tr key={c.id}><td><strong>{c.name}</strong></td><td>{c.phone||"—"}</td><td>{c.email||"—"}</td><td>{c.address||"—"}</td><td>{formatDate(c.created_at)}</td></tr>)}</tbody></table>}</div></div>}
