import {notFound}from "next/navigation";import Link from "next/link";import {deleteOrder,updateDelivery,updateOrder}from "@/app/actions";import {getOrder}from "@/lib/data/orders";import {getAdminStaff}from "@/lib/data/admin-staff";import {formatCurrency,formatDate,getPriority}from "@/lib/utils";import {StatusBadge}from "@/components/status-badge";
export const dynamic="force-dynamic";
export default async function OrderDetailPage({params}:{params:Promise<{id:string}>}){const{id}=await params;const[order,admins]=await Promise.all([getOrder(id),getAdminStaff()]);if(!order)notFound();const priority=getPriority(order);return <div className="page-wrap"><header className="page-header split"><div><Link href="/orders" className="eyebrow">← Orders</Link><h1>{order.order_number}</h1><p className="subtitle">Created {formatDate(order.created_at)} · {order.customer?.name}</p></div><div className="stack"><StatusBadge value={order.status}/>{order.isOverdue&&<span className="badge unpaid">Delivery overdue</span>}</div></header>
<div className="detail-grid"><div className="stack"><section className="panel"><div className="section-heading"><div><p className="eyebrow">Order</p><h2>Details & status</h2></div><div className={`priority-dot ${priority.tone}`}><strong>{priority.score}</strong><span>score</span></div></div><form action={updateOrder} className="panel-body"><input type="hidden" name="id" value={order.id}/><div className="form-grid"><div className="field"><label>Status</label><select name="status" defaultValue={order.status}><option value="pending">Pending</option><option value="confirmed">Confirmed</option><option value="in_progress">In progress</option><option value="completed">Completed</option><option value="cancelled">Cancelled</option></select></div><div className="field"><label>Payment</label><select name="payment_status" defaultValue={order.payment_status}><option value="unpaid">Unpaid</option><option value="partial">Partial</option><option value="paid">Paid</option></select></div><div className="field"><label>Total amount (MYR)</label><input name="total_amount" type="number" min="0" step=".01" defaultValue={order.total_amount}/></div><div className="field"><label>Assigned admin</label><select name="assigned_admin_id" defaultValue={order.assigned_admin_id||""}><option value="">Unassigned</option>{admins.map(a=><option key={a.id} value={a.id}>{a.name}</option>)}</select></div><div className="field full"><label>Items & notes</label><textarea name="notes" defaultValue={order.notes||""}/></div></div><div className="form-actions" style={{marginTop:20}}><span></span><button className="button primary">Save order changes</button></div></form></section>
{order.delivery&&<section className="panel"><div className="section-heading"><div><p className="eyebrow">Delivery</p><h2>Schedule & progress</h2></div><StatusBadge value={order.delivery.status}/></div><form action={updateDelivery} className="panel-body"><input type="hidden" name="delivery_id" value={order.delivery.id}/><input type="hidden" name="order_id" value={order.id}/><div className="form-grid"><div className="field"><label>Scheduled date</label><input name="scheduled_date" type="date" required defaultValue={order.delivery.scheduled_date}/></div><div className="field"><label>Delivery status</label><select name="delivery_status" defaultValue={order.delivery.status}><option value="scheduled">Scheduled</option><option value="out_for_delivery">Out for delivery</option><option value="delivered">Delivered</option><option value="failed">Failed</option><option value="missed">Missed</option></select></div><div className="field full"><label>Delivery notes</label><textarea name="delivery_notes" defaultValue={order.delivery.notes||""}/></div></div><div className="form-actions" style={{marginTop:20}}><span></span><button className="button primary">Save delivery changes</button></div></form></section>}</div>
<aside className="stack"><section className="panel"><div className="section-heading"><div><p className="eyebrow">Customer</p><h2>Contact</h2></div></div><div className="panel-body detail-list"><div className="detail-item"><span>Name</span><strong>{order.customer?.name}</strong></div><div className="detail-item"><span>Phone</span><strong>{order.customer?.phone||"—"}</strong></div><div className="detail-item"><span>Email</span><strong>{order.customer?.email||"—"}</strong></div><div className="detail-item"><span>Address</span><strong>{order.customer?.address||"—"}</strong></div></div></section><section className="panel"><div className="panel-body"><p className="eyebrow">Summary</p><div className="detail-list" style={{marginTop:18}}><div className="detail-item"><span>Order total</span><strong>{formatCurrency(order.total_amount)}</strong></div><div className="detail-item"><span>Delivery date</span><strong>{formatDate(order.delivery?.scheduled_date)}</strong></div></div></div></section><section className="panel"><div className="panel-body"><p className="eyebrow">Danger zone</p><p className="subtitle" style={{margin:"10px 0 16px"}}>Deleting an order also removes its delivery.</p><form action={deleteOrder}><input type="hidden" name="id" value={order.id}/><button className="button danger" type="submit">Delete order</button></form></div></section></aside></div></div>}
