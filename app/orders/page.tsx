import Link from "next/link";import {getOrders} from "@/lib/data/orders";import {formatCurrency,formatDate} from "@/lib/utils";import {StatusBadge} from "@/components/status-badge";
export const dynamic="force-dynamic";
export default async function OrdersPage({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){const params=await searchParams;let orders=await getOrders();if(params.status)orders=orders.filter(o=>o.status===params.status);if(params.payment)orders=orders.filter(o=>o.payment_status===params.payment);return <div className="page-wrap"><header className="page-header split"><div><p className="eyebrow">Operations</p><h1>Orders</h1><p className="subtitle">Every active and completed customer order.</p></div><Link className="button primary" href="/orders/new">+ New order</Link></header><form className="toolbar"><select name="status" defaultValue={params.status||""}><option value="">All statuses</option><option value="pending">Pending</option><option value="confirmed">Confirmed</option><option value="in_progress">In progress</option><option value="completed">Completed</option><option value="cancelled">Cancelled</option></select><select name="payment" defaultValue={params.payment||""}><option value="">All payments</option><option value="unpaid">Unpaid</option><option value="partial">Partial</option><option value="paid">Paid</option></select><button className="button">Apply filters</button></form><div className="section-block table-wrap">{orders.length===0?<div className="empty-state"><span>+</span><h3>No orders yet</h3><p>Create your first order to start tracking deliveries.</p><Link href="/orders/new" className="button primary">Create order</Link></div>:<table className="data-table"><thead><tr><th>Order</th><th>Customer</th><th>Status</th><th>Payment</th><th>Delivery</th><th>Assigned</th><th>Amount</th></tr></thead><tbody>{orders.map(o=><tr key={o.id}><td><Link href={`/orders/${o.id}`}><strong>{o.order_number}</strong></Link></td><td>{o.customer?.name}</td><td><StatusBadge value={o.status}/></td><td><StatusBadge value={o.payment_status}/></td><td className={o.isOverdue?"danger-text":""}>{o.delivery?formatDate(o.delivery.scheduled_date):"Not set"}</td><td>{o.assigned_admin?.name||"—"}</td><td>{formatCurrency(o.total_amount)}</td></tr>)}</tbody></table>}</div></div>}
